# ontorag graph backend (the demo only exercises the default Fuseki path)
GRAPH_STORE=fuseki
FUSEKI_URL=http://localhost:3030
FUSEKI_DATASET=ontorag

# Optional: keep the schema scoped under a named graph identity
DEMO_ONTOLOGY=manufacturing-demo
